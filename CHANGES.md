# Changes Logs

## v2.0.0

- chore: update dependencies
- build: migrated to ESM modules
- doc: update the documents for v2.0.0 release
- build: added `exports` field in `package.json` for better module resolution

## v1.0.1

- fix(enc:wasm): should copy the result from wasm memory instead of return the reference to the memory
